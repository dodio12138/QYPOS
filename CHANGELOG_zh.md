# 更新日志

<p align="center">
  <a href="./CHANGELOG.md"><img src="https://img.shields.io/badge/lang-English-blue.svg" alt="English"></a>
  <a href="./CHANGELOG_zh.md"><img src="https://img.shields.io/badge/lang-中文-red.svg" alt="中文"></a>
</p>

本文件记录 QYPOS 的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)，
本项目遵循 [语义化版本](https://semver.org/spec/v2.0.0.html)。

---

## [Unreleased]

- feat: 新增网站已付款在线订单收件箱、幂等导入、SSE 断线重连/游标恢复 Connector，以及后台只读原始订单查看；M1 不创建正式订单或付款记录
- feat: 网站订单到达时新增 POS/后台醒目弹窗、提示音、收件箱自动刷新和运维测试按钮；确认后可按 JSON 快照打印简易后厨单
- feat: 在线订单 Connector 默认仅在 Europe/London 11:00–22:05 营业时段检测，非营业时间停止 SSE 与重连
- fix: 前台 WebSocket 跟随 `/api-proxy` 或 `NEXT_PUBLIC_WS_URL` 配置，API 增加连接与在线订单广播日志，页面显示实时连接状态；后台菜单名称更新为“网站”和“外卖”

- fix: 外卖对账页面改为单列流程布局，独立处理 Deliveroo/Uber Eats 状态；过期连接会明确提示并允许清理，不再因一个平台状态异常阻塞另一个平台
- fix: 将 Deliveroo 与 Uber Eats 同步按钮合并到统一的“营业额同步”区域，共用日期时间范围，不再单独创建 Uber Eats 同步卡片
- fix: 修复分析面板历史区间数据展示，补齐整段日期序列、修正整月环比范围，并忽略较早请求对当前报表的覆盖
- fix: 外卖营业额按每笔订单的 placed_at（英国本地日期）归属月份，不再按跨月同步批次的 business_date 汇总
- refactor: 移除服务器端外卖账号密码自动登录与无头浏览器服务，外卖 token/Cookie 改为后台手动保存
- feat: 外卖对账新增每天 14:00、23:00 自动同步开关，默认关闭，关闭后仅执行手动同步

- feat: 接入 Uber Eats 历史订单只读同步，支持加密保存后台 Cookie、分页游标、手动/可选 14:00/23:00 自动同步、按订单号去重，并将取消单排除在营业额之外

### Added
- **Deliveroo 营业额对账**：后台新增只读 Deliveroo 网页会话同步，按时段保存去重快照和已送达订单营业额，不会把外卖订单混入 POS 收款流程。
- **Deliveroo 数据分析接入**：今日看板和销售分析新增外卖营业额、已送达订单、外卖现金指标；跨自动/手动时段按外卖订单号去重，避免重复计算。
- **同步到当前时间**：同步今天时自动从 00:00 拉取到按钮点击时刻，历史日期仍可使用自定义时段。
- **Deliveroo Token 有效期校验**：连接保存时校验 JWT 实际过期时间，缓存会话不会再显示为 12 小时有效而实际已经失效。
- **Deliveroo Token 持久化**：token 现在使用 AES-GCM 加密保存到 PostgreSQL，Redis 仅作缓存；API 重启后可自动恢复，不需要每次重新输入。
- **外卖同步时间范围**：手动同步支持独立设置开始/结束日期与时间，并保留“一键同步到当前时间”。
- **Deliveroo 分页兼容**：同步优先请求更大的订单页，并兼容更多分页游标字段，避免订单超过 20 笔时误报“没有分页游标”。
- **跨日分页修复**：Deliveroo 现在按自然日分段请求，再按精确开始/结束时间过滤，绕过上游固定 20 笔且不返回游标的限制。
- **外卖取消单与趋势**：取消单保留在同步明细但不计入营业额；分析页新增取消单指标，并按环比/同比对外卖指标显示真实趋势。
- **外卖分析面板样式统一**：移除外卖区域专用背景和白色强制样式，恢复与原分析面板一致的卡片结构和视觉层级。
- **看板外卖指标卡**：看板外卖指标改为复用原生数据卡，并支持与昨日的真实趋势对比。
- **每日会计支付拆分**：会计 Excel/CSV 的每日汇总新增现金与银行卡实收入账分列统计。
- **现金 + 银行卡混合支付**：普通收款流程支持按任意金额连续收取多笔付款；首笔付款后保留收款页并实时显示已收与待收，剩余金额可使用手工银行卡或 Dojo 完成。
- **中英双语会计 CSV 与 Excel**：销售导出包含中英文口径与公式、会计汇总、支付对账、每日汇总和已结账订单账簿；Excel 提供四个带格式和冻结表头的工作表，CSV 保留 UTF-8 BOM 与可直接求和的数字单元格。
- **可选时段累计趋势**：30/60 分钟图表新增默认关闭且相互独立的“累计单量”和“累计营业额”按钮，并加入与每日趋势图一致的悬浮定位线和按已显示指标反馈的提示浮层。
- **实际出勤 OFF 与 120 分钟休息**：员工实际出勤记录现在可明确选择 `OFF`，实际工时按 0 计算且保留备注；计划排班和实际出勤的休息时间预设均新增 120 分钟。
- **免单结账**：POS 收款页新增管理员授权的 0 元免单结账，强制填写原因并可追加备注；订单以 `paid` 状态和 `complimentary` 付款记录完成，报表新增免单数。
- **现金保留实收**：现金收款可选择“无需找零，全部计入实收”；系统分别记录订单结算金额、保留金额和实收入账，报表新增“实收入账”指标。
- **已付款订单金额调整**：后台订单详情支持对已结账订单进行管理员授权的金额调低、退款差额提示和追加备注；收银员需通过管理员账号与 PIN 临时授权。
- **PBKDF2 PIN 哈希**：使用 Node.js 内置 `crypto.pbkdf2Sync`（SHA-512，10万次迭代，随机盐值）对员工 PIN 进行哈希存储。旧明文 PIN 在首次登录时自动升级，对用户透明无中断。
- **PIN 隐私保护**：`GET /users` API 不再返回 `pin` 字段；后台账户管理页面不再显示 PIN 明文。
- **账户管理界面重构**：卡片式布局、角色彩色徽章（Owner🔴/Cashier🔵/Kitchen🟢）、行内编辑/重置PIN、行内删除确认、PIN 显示/隐藏切换、PIN 确认两次输入。
- **PIN 哈希单元测试**：9 个测试用例覆盖 hash/verify/upgrade/边界情况全流程。
- **共享测试辅助模块** (`tests/helpers.mjs`)：统一 API 请求封装、登录、环境管理、清理工具。
- **AGENTS.md**：项目级 AI 编码助手指南，包含技术栈、构建命令、架构决策、常见陷阱。
- **员工管理**：员工增删改查、排班、考勤记录、时薪管理。
- **排班系统**：周排班计划器，支持拖拽时间预设、实际出勤记录和工时转化率（营业额/小时）指标。
- **角色权限**：精细化收银员权限集；服务费调整/豁免、折扣、订单取消等操作均需显式授权。
- **POS 登录门禁**：前台所有操作（开台、加菜、打印、收款、清台）均需员工登录验证。
- **下单确认步骤**：堂食开台与外带创建前增加确认弹窗，防止误触。
- **Dojo Go 刷卡终端**：集成 Dojo Go 支付终端，支持 Pay-at-Counter 模式（Payment Intent + 终端会话生命周期）。
- **严格打印机路由**：厨房/收银指定打印机缺失、停用或无 IP 时直接报错，不再静默降级到其他设备。
- **仅新增菜品厨打锁定**：只锁定并打印新增菜品，已厨打的菜品不会重复出单。
- **热销菜下钻**：数据看板热销菜品支持多选合并趋势图，兼容历史数据中缺失 `item_id` 的情况。
- **报表增强**：新增星期几多选筛选；日期预设扩充为今日、昨天、近 7 天、近 30 天、本月、本周、上周、上月。
- **报表分析构成图**：数据分析新增售出单品、服务费、折扣、VAT 汇总，菜单分类点单次数/营业额构成图，联动高亮与可隐藏趋势虚线。
- **每日趋势悬浮卡片**：每日营业额/单量/客单价趋势图悬停信息改为日期 + 星期几卡片，并显示当前数据点相对前一个数据点的涨跌百分比。
- **每日趋势纵轴刻度**：每日趋势图新增整数等距纵轴刻度与横向网格线，营业额纵轴最大值会根据当前数据自动贴近取整。
- **指标卡片新版式**：后台看板和数据分析的数字卡片新增指标图标、右上角趋势百分比，以及主数字下方的实际变化量。
- **后台菜单体验优化**：菜单管理增加概览统计、搜索、上下架筛选、菜品上一页/下一页分页，并将备注词条管理升级为独立预设区域，支持按菜单分类设置适用范围。
- **完整多语言**：前台与后台界面完整覆盖中文 / 英文文案。
- **ESLint 配置**：根级扁平化 ESLint 配置，核心规则 `no-undef` 已接入 CI 流水线，可自动捕获 prop 传递遗漏类回归问题。
- **审计日志筛选**：后台审计日志支持用户、操作类型和精确时间范围组合筛选。
- **敏感设置二次验证**：修改税率等敏感设置需当前账号与 PIN 二次确认。
- **订单分页**：后台订单列表按每页 20 条分页浏览。

### Fixed
- **钱箱开柜防重复**：钱箱请求新增按用户/终端/打印机维度的 3 秒冷却，前端按钮冷却提示、来源审计信息，以及网络打印机瞬时超时后延迟 1 秒重试一次。
- **会计 Excel 金额格式**：每日汇总中的订单总额现在按两位小数货币格式显示，不再使用整数格式。
- **班表指标按当前时间计算**：工时转化率和工资占比现在只使用截至当前分钟已发生的有效工时及折算工资成本；周合计改为对已有每日比率求平均，未来排班不再影响当前效益。
- **0 元订单结账**：订单总额确实已归零时现在可通过专用的 0 元付款记录完成结账，同时仍会拦截尚有应收金额的订单提交 0 元付款。
- **排班自动隐藏失效**：修复“全部展开”留下永久员工例外，导致新增员工后“自动折叠空/OFF”开关看似失效的问题；重新启用时会恢复统一过滤。
- **未结账订单进入营业统计**：排班营业额、看板和销售报表现在只统计 `paid` 订单，取消单和仍在进行中的订单不再计入。
- **订单明细行金额**：后台订单详情现在由 API 返回每个菜品行的明细金额，修复菜品金额显示为 0 的问题。
- **分单父单详情**：后台订单详情中，分单后的父单现在会显示关联子单号及对应菜品明细。
- **排班营业额统计**：排班表营业额现已排除分单后的原始父单，避免分单订单重复计入营业额。
- **备注词条分类范围保存**：兼容历史菜单分类 UUID 格式，修复点击单个分类后刷新又回到“全部分类”的问题。
- **备注词条范围选择**：备注词条适用分类改为整颗按钮点击，修复点击单个分类应用时没有明显反应的问题。
- **夏令时日期偏移**：单品趋势图表日期轴现已与主报表营业额日期完全对齐，不再因夏令时提前或延后一天。
- **`locale` prop 崩溃**：修复了 `PosLogin`、`DiscountAdminModal`、`SettingsView` 等多个组件因 `locale` 属性未声明或未传递而导致的 `ReferenceError` 崩溃，已通过 ESLint 规则防护。
- **折扣封顶**：折扣金额现已封顶至订单小计，防止出现负数或异常总额。

### Changed
- **付款校验**：付款金额必须大于 0，找零不能为负，已关闭订单拒绝重复收款。
- **备份调度**：后台运维面板现已支持手动备份、自动备份计划、备份文件列表和下载。

---

## [0.1.0] - 2026-06-25

### Added — MVP 初始版本

#### �️ 后台体验
- 可收起侧栏导航：点击 Logo 切换 220px ↔ 72px，状态持久化到 localStorage
- 收起/展开双向平滑宽度动画（200ms ease）
- 两种状态下图标尺寸、按钮间距、内边距完全一致

#### �🛎️ 点餐前台
- 可视化餐桌地图展示与状态管理
- 堂食开台 + 外卖下单双模式
- 菜品分类浏览、规格选择、加料组与备注
- 订单加菜、折扣、服务费调整
- 手动支付记录（现金/刷卡/扫码/其他）
- WebSocket 实时桌台状态更新

#### 🖨️ 后厨打印
- ESC/POS 网络打印机支持
- 厨房单与收银小票拆分打印
- 多打印机路由（厨房/收银/吧台独立配置）
- 打印失败自动重试机制
- 厨房菜品级制作状态追踪
- 打印任务管理后台

#### ⚙️ 后台管理
- 菜单全量管理：分类、菜品、规格、加料组 CRUD
- 可视化餐桌布局编辑器（拖拽、区域管理、复制/删除桌台）
- 网格吸附、撤销/重做
- 系统设置：税率、服务费、币种、打印机配置
- 收银小票预览

#### 📊 数据看板
- 今日营业额、订单数、客单价、Tax、服务费统计
- 热销菜品排行
- 历史销售报表（日期筛选 + CSV 导出）
- 审计日志预览

#### 🔧 运维
- 数据库手动备份 / 自动备份计划
- 备份文件下载
- 服务健康检查面板
- 浏览器离线/断网状态提示

#### 🧪 测试
- 金额计算单元测试（含税/未税、折扣、服务费）
- API 集成测试（可选执行）

#### 🏗 基础设施
- Docker Compose 一键部署
- PostgreSQL 16 + Redis 7
- Fastify API + WebSocket
- Next.js 14 前端
- Node.js 打印 Worker
